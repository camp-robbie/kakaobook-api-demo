spring:
  application:
    name: my-pocket
  datasource:
    url: jdbc:mysql://${db.host}:3306/${db.name}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: ${db.username}
    password: ${db.password}

kakao:
  restapi:
    key: KakaoAK ${api.kakao}
  api:
    baseurl: https://dapi.kakao.com
    path:
      book: /v3/search/book